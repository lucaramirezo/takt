packages:
  - "apps/web"
  - "apps/api"
  - "packages/*"
  # apps/mobile is initialized later via create-expo-app (Expo SDK 54 native pins);
  # excluded from the build graph until the worker/kiosk clock-mode slices are built.
